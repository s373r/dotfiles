{{ if ne .chezmoi.osRelease.id "fedora" -}}
{{     fail "Only Fedora is supported at the moment!" }}
{{ end -}}

sudo dnf install meson -y \
    git \
    libinput-devel libudev-devel # fix-touchpad-speed.tmpl: dependencies for libinput-config

{{ if eq (env "FIX_TOUCHPAD_SPEED") "yes" }}
{{     template "fix-touchpad-speed.tmpl" . }}
{{ end }}
