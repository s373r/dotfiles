[user]
    name = Dima Pristupa
    email = dima@pristupa.dev

[init]
    defaultBranch = main

[pull]
    rebase = true

[alias]
    # NOTE: based on
    #       https://gist.github.com/ecasilla/9669241
    hist = log --pretty=format:'%C(yellow)[%ad]%C(reset) %C(green)[%h]%C(reset) | %C(red)%s %C(bold red){{%an}}%C(reset) %C(blue)%d%C(reset)' --graph --date=short
